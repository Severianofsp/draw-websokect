const WS_URL = 'ws://draw-api.onrender.com';

const STATUS = {
  WIN: 'youwin',
  LOSE: 'youlose',
};

const ACTIONS = {
  ADMIN: 'admin',
  DRAW: 'draw',
  CLIENT_COUNT_UPDATE: 'clientCountUpdate',
};
